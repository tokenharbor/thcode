// Token Harbor release version string shown in the TUI footer.
// Bump per release. CI workflow doesn't override this — it's the
// "user-visible product version" of thcode, decoupled from upstream
// opencode's own semver.
export const THCODE_VERSION = "0.1.0"
